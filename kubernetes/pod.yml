apiVersion: v1
kind: Pod
metadata:
  name: my-pod-1
  labels:
    app: my-pod-1
  namespace: ns-demo
spec:
  containers:
    - name: my-pod-1
      image: httpd
      ports:
        - containerPort: 80
